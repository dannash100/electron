{
  "name": "markdown",
  "productName": "Markdown Editor",
  "version": "1.0.0",
  "main": "./app/main.js",
  "group": {
    "kind": "build",
    "isDefault": true
  },
  "scripts": {
    "start": "electron .",
    "build": "electron-packager . --overwrite --out=build --icon=icons/Icon.icns",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "MIT",
  "dependencies": {
    "electron": "^3.0.4",
    "marked": "^0.5.1"
  },
  "devDependencies": {
    "electron-packager": "^12.2.0"
  }
}
